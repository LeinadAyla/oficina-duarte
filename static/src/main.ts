document.addEventListener("DOMContentLoaded", () => { const clienteModalElement = document.getElementById("clienteModal"); if (clienteModalElement) { clienteModalElement.addEventListener("show.bs.modal", (event: any) => { const button = event.relatedTarget as HTMLElement; if (button) { const id = button.getAttribute("data-bs-id"); const nome = button.getAttribute("data-bs-nome"); const cpf = button.getAttribute("data-bs-cpf"); const email = button.getAttribute("data-bs-email"); const telefone = button.getAttribute("data-bs-telefone"); const endereco = button.getAttribute("data-bs-endereco"); const modalTitle = clienteModalElement.querySelector(".modal-title"); const form = clienteModalElement.querySelector("form") as HTMLFormElement; if (id) { if (modalTitle) modalTitle.textContent = "Editar Cliente"; if (form) form.action = `/cliente/editar/${id}`; (document.getElementById("nome") as HTMLInputElement).value = nome || ""; (document.getElementById("cpf") as HTMLInputElement).value = cpf || ""; (document.getElementById("email") as HTMLInputElement).value = email || ""; (document.getElementById("telefone") as HTMLInputElement).value = telefone || ""; (document.getElementById("endereco") as HTMLInputElement).value = endereco || ""; } else { if (modalTitle) modalTitle.textContent = "Novo Cliente"; if (form) { form.action = "/cliente/novo"; form.reset(); } } } }); } });
